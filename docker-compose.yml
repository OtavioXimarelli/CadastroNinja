version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: postgresBD
    environment:
      POSTGRES_DB: ninjasDB
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5431:5432"
    restart: always

